import { HeightConverterPipe } from './height-converter.pipe';

describe('HeightConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
